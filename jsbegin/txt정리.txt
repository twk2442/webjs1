CSS 
 style 태그 , div 나 span 태그 사용
둘 차이 div 는 화면 전체를 사용 따라서 감싼 내용 이후 줄바꿈 but span은 전체 사용 x


선택자: //property
우선순위 : 태그<class<id ...
타겟팅목적은  id 개별에 사용 / 구별에 용이 
class  style 태그에서 지정할때  . 붙인다. 
class는 그룹핑  여러개 태그 묶어서 관리에 용이 

id는 단독적 구분, 예외처리 

js

비교연산자
=== 

최대한 중복을 제거하려고 노력 
-->     this  사용 

js 에서 배열 문법  var 변수 =[   ,  , ];  대괄호 사용 
      
js 
함수 객체 , 파일을 이용하면 코드 유지보수 용이하다. 

배열 : 순서대로 정보를 담는 그릇 

객체: 순서 상관없이 정보 담는다 ( 이름 유)

객체 생성 : 

var 객체변수이름 = { 
	"이름":"내용",
	"이름":"내용",

 } 

 객체 값으로 함수도 가능 
따라서 객체에 소속된 함수는 메소드 
객체에 소속된 변수는 프로퍼티

immutability
??

보통 변수에서 가변은 좋다 but 불변이 꼭필요한 부분에있어서는 불변성 필요 

객체는 값자체를프로퍼티를 통해서 바꿀 수 있다 .

객체 복사 // assign사용 ex: Object.assign(,바꿀객체)

중첩된 객체의 복사는 불변성 보장 x 
객체의 값이 프로퍼티(또다른 객체)인 경우 그 객체또한 복제해야함// concat사용(배열의경우)

Object.freeze(변수또는 객체) 를 통해 객체를 얼려서 불변 방지 가능 
but 변수내에서의 객체는 얼려지지 않음 따라서 freeze로 또한번 그 객체의 키값을 
얼려주면 완벽하게 불변할 수 있다.

const vs freeze 
const는 변수가 가리키는 대상의 변화(할당 = 연산자사용)를 막음 // 이름과 값을 고정
freeze는 변수가 가리키는 값의 직접적 변화를 막음 //값 자체를 바꾸는것을 막음


